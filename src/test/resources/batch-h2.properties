# Placeholders batch.*
#    for HSQLDB:
batch.jdbc.driver=org.h2.Driver
# use this one for a separate server process so you can inspect the results
# (or add it to system properties with -D to override at run time).
batch.jdbc.url=jdbc:h2:tcp://localhost/~/batch-gemfire
batch.jdbc.user=sa
batch.jdbc.password=
batch.drop.script=/org/springframework/batch/core/schema-drop-hsqldb.sql
batch.schema.script=/org/springframework/batch/core/schema-hsqldb.sql
batch.business.schema.script=business-schema-h2.sql
batch.data.source.init=true
